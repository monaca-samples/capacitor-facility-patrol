import React from 'react'

import { {{pascalCase name}} } from './{{dashCase name}}.markup'
import { {{pascalCase name}}ContainerProps } from './{{dashCase name}}.types'

export const {{pascalCase name}}Container = ({ dummyProp }: {{pascalCase name}}ContainerProps) => {
return <{{pascalCase name}} />
};
