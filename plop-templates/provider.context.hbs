import { createContext } from 'react'

import { {{pascalCase name}}ContextType } from './{{dashCase name}}-provider.types'

export const {{pascalCase name}}Context = createContext<{{pascalCase name}}ContextType | undefined>(undefined)